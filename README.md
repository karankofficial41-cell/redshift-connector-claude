# redshift-connector-claude
Settings → Extensions Click Advanced settings → find the Extension Developer section Click Install Extension… and select the .mcpb (Alternatives: double-click the .mcpb, or drag it onto the Claude Desktop window — not into a drop zone inside Settings)
